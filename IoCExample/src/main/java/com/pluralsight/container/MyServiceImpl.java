package com.pluralsight.container;

public class MyServiceImpl implements MyService {

    public MyServiceImpl(Long type, MonitorDao monitorDao, int value, MonitorServiceImpl monitorServiceImpl, String message) {
        int t = 3;
    }
}
